ManutBR:
  target: ci
  outputs:
    ci:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID', 'ci-dummy') }}"
      dataset: "{{ env_var('BQ_DATASET', 'ci_dummy') }}"
      location: "{{ env_var('BQ_LOCATION', 'US') }}"
      threads: 1
